# Personal notes (private by design — never tracked)
notes/

# Obsidian app state + plugin configs (machine-specific, not wiki content)
.obsidian/

# Smart Connections runtime data (embeddings/logs — churns constantly, not meaningful history)
.smart-env/
